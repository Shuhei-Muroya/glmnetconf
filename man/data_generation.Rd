% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_generation.R
\name{data_generation}
\alias{data_generation}
\title{Generate Synthetic Data for lasso Regression}
\usage{
data_generation(
  N_train = 1500,
  N_test = 100,
  p = 800,
  rho = 0.5,
  sparse_rate = 0.5,
  sigma = 1,
  seed = 1
)
}
\arguments{
\item{N_train}{Integer; the number of observations for the training set.}

\item{N_test}{Integer; the number of observations for the test set.}

\item{p}{Integer; the total number of predictors (features).}

\item{rho}{Numeric; the constant correlation coefficient between predictors (0 to 1).}

\item{sparse_rate}{Numeric; the proportion of true coefficients set to zero (0.0 to 1.0).}

\item{sigma}{Numeric; the standard deviation of the Gaussian noise (error term).}

\item{seed}{Integer; the random seed for reproducibility.}
}
\value{
A list containing:
\itemize{
\item \code{X_train}: A matrix of training predictors.
\item \code{y_train}: A numeric vector of training responses.
\item \code{X_test}: A matrix of test predictors.
\item \code{y_test}: A numeric vector of test responses.
\item \code{beta}: The true coefficient vector used in the data generation.
}
}
\description{
Generates simulation datasets with a compound symmetry covariance structure.
}
\details{
The predictors are generated from a multivariate normal distribution \eqn{N(0, \Sigma)},
where \eqn{\Sigma} is a covariance matrix with 1 on the diagonal and \eqn{\rho}
on all off-diagonal elements. The true coefficient vector \eqn{\beta} consists
of ones for the first \eqn{p \times (1 - sparse\_rate)} elements and zeros thereafter.
}
